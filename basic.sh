#--> touch (to create a file)
# code . --> to open visual studio. (.) open cuurent folder
# pwd --> current working directory

# to resolve DNS to IP address.
# nslookup <domain>
#top --> to check running process
#htop --> to checck running procecss + thread (shft+H --> only thread or process)
# to check line in  file
# wc -l filename
#man to check detail page of the command
# eg: -w noof words
# wc -w <filename>
#df -h --> to check disc/storage usage
